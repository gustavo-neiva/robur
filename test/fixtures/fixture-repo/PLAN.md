# Plan — ratchet fixture repo

> A tiny tracker used by `test/selftest.sh` to exercise the loop end-to-end with
> the fake-agent (no api keys). Three open tasks; each `ratchet once` turn ticks
> one `[x]` and commits it green.

## Milestone 0 — Walking skeleton
- [ ] T0.1 (trivial) first task
- [ ] T0.2 (trivial) second task
- [ ] T0.3 (trivial) third task
