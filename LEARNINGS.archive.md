# Learnings — ARCHIVE (closed-milestone notes)

> Moved out of LEARNINGS.md on 2026-09-03 (audit Q9: static context floor —
> 24.5KB re-read every turn). Closed-milestone narratives; the actionable
> gotchas live on in code comments and the slimmed LEARNINGS.md.

- M1 self-QA (T1.7): both gates green — unit gate 4 runs / 9 assertions, 0 failures; diff gate `0 diffs across 3 scenarios` (help, unknown-flag, doctor). Harness invocation lives in AGENTS.md under Gates.
- M1: `exe/robur` resolves `lib/` beside itself, so a differential mutant must copy BOTH `exe/robur` and `lib/` into the shadow tree; mutating a lib copy while pointing candidate_cmd at the real exe silently tests the unmutated code.
- M2: `Commands.doctor_report`'s tier-routing block was hardcoded static text regardless of conf — wired it through `Tier.chain_for`/`Tier.thinking_for` on the repo-only `conf_values` (not full `Config.load`, matching how `AGENT_CMD`/`VERIFY_CMD` already read doctor's conf).
- T6.6 (models_cmd): ports `list`/`add`/`remove`/`thinking`/`rank`; `rank`/`list`'s derived-chain suggestion always takes bash's own "no cost signal" branch (no model-cost port) — a `ponytail:` comment in the file names the upgrade path. `pi_model_registry` rescues `Errno::ENOENT` instead of a separate `command -v pi` probe.
- T6.6 (wait_for_merge / open_milestone_pr): MERGE_POLL_SECS/MERGE_WAIT_TIMEOUT have NO global default in bash — inline `${VAR:-N}` at the one call site, mirrored, not invented as Config defaults. bash's `tracker_milestone_completed_list` keeps the `[x]` marker (only strips the leading `-`), so it's a separate method from `Plan#completed_list`. Tested with injected FakeRepo/FakeGh doubles, not PATH-stubbed scripts.
- T6.6 (fanout): `fanout_independent_milestones`' slug is NOT lowercased (tracker.sh awk does no tolower) — a first draft wrongly assumed lowercase. Launch/wait steps are injectable procs; bash's own suite 36 never launches real parallel loops either. `Process.wait` (no args) is Ruby's `wait -n`; reaped pids must be tracked so the final sweep doesn't re-`Process.waitpid` and raise `Errno::ECHILD`.
- T6.6 (auto_plan_pr0): reuses `Commands.plan_turn` (bash calls the same `plan_turn` helper); on failure it `exit`s directly, skipping pid-file/loop/epilogue — ported as an Integer return the caller returns immediately. PR #0 body is scoped to the TRACKER_FILE diff's added lines only.
- T6.6 (milestone_complete_check / run_review_turn): `run_review_turn`'s thinking arg is the JUST-FINISHED build turn's `Tier.thinking_for` value — bash never calls thinking_for_tier "review" in the main loop (quirk preserved). `Loop.commit_review_injected_tasks` gates on a REAL `File.directory?(".git")` even with an injected fake repo — tests need `FileUtils.mkdir_p(dir/.git)`.
- T6.7 (M6 self-QA): milestone-6 suite reports `0 diffs across 11 scenarios (1 unsupported)`; `models list` is `unsupported(name:, reason:)` (both sides shell out to live `pi --list-models`). Surfaced five real gaps: missing `stats` dispatch arm; `Loop.run` banner missing lines `cmd_once` had; `Loop.run` never wrote loop.pid (new `<pid>` normalizer); `write_note` missing the always-derived GREEN/RED first line; `Loop.run`/`Commands.plan_turn` missing `show_excerpt` + partial-conf `TypeError` on nil token fallbacks. `cmd_init`/`cmd_new`/`cmd_plan` leaked helper return values as exit codes — grep every `cmd_*` for a bare non-`0` final expression.
